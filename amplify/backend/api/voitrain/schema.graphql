type CardSet @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String
  image_url: String
}

type Card @model @auth(rules: [{allow: public}]) {
  id: ID!
  word: String
  image_url: String
  cardsetID: String
}
 